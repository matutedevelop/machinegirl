# Hyprland

All or almost all of my hyperland configuration is written in Nix Language inside `home.nix`


# DotFiles

inside configs there is every config of external packages that I use (Some are still missing). Then I mannage this configs rewriting its content to the respective `~/.config` file.
This is done inside `home.nix` in the dotfiles secction almost at the bottom
