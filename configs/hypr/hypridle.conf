general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
}

listener {
    timeout = 20
    on-timeout = hyproled
    on-resume = hyproledo
}

listener {
    timeout = 90
    on-timeout = hyprlock
}


